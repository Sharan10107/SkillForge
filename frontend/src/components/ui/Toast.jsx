// Toasts are rendered globally via <Toaster /> in ToastContext.jsx.
// This file exists as the documented place to add custom toast
// content components (e.g. an "undo delete" toast) as the app grows.
export default function ToastPlaceholder() {
  return null;
}
